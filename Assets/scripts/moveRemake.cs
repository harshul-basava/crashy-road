using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class moveRemake : MonoBehaviour
{
    // bool created;
    // public GameObject empty;
    // public GameObject re;
    // Start is called before the first frame update
    void Start()
    {
       //created = false;
    }

    // Update is called once per frame
    void Update()
    {
        // gameObject.transform.Translate(0, 0, -0.1f);
        // if (gameObject.transform.position.z <= 100 && created == false) 
        // {
        //     GameObject movingGround = Instantiate(re, transform.position + new Vector3(0, 0, 400), transform.rotation) as GameObject;
        //     GameObject emptyGround2 = Instantiate(empty, transform.position + new Vector3(0, 0, 300), transform.rotation) as GameObject;
        //     GameObject emptyGround1 = Instantiate(empty, transform.position + new Vector3(0, 0, 200), transform.rotation) as GameObject;
        //     GameObject emptyGround = Instantiate(empty, transform.position + new Vector3(0, 0, 100), transform.rotation) as GameObject;
        //     created = true;
        // }
        // if (gameObject.transform.position.z <= -300) 
        // {
        //     Destroy(gameObject);
        // }
    }
}
